// Firestore chat removed - chat disabled
// Removed chat request import

class ChatService {
  //
  static sendChatMessage(String message, dynamic chatEntity) async {
    print("Chat messaging disabled - Firebase removed");
    return false;
  }
}
